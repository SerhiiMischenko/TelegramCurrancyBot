on: [push]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Java
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'

    - name: Stop the Running Project
      run: |
        echo "$SSH_PRIVATE_KEY" > ssh_key.pem
        if pgrep -f "telegram-bot-get-currency-rate-0.0.1-SNAPSHOT.jar" > /dev/null; then
        ssh -i ssh_key.pem -o StrictHostKeyChecking=no ec2-3-69-234-225.eu-central-1.compute.amazonaws.com "pkill -f telegram-bot-get-currency-rate-0.0.1-SNAPSHOT.jar"
        fi
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

        
    - name: Build with Maven
      run: mvn clean install -DskipTests

    - name: Deploy to AWS
      run: |
        scp -i ${{ secrets.SSH_PRIVATE_KEY }} /Users/serhiimischenko/IdeaProjects/TelegramCurrancyBot/target/telegram-bot-get-currency-rate-0.0.1-SNAPSHOT.jar ubuntu@3.69.234.225:/home/ubuntu/
